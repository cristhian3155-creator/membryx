<nav class="bg-white border-b border-slate-200">
  <div class="max-w-6xl mx-auto px-4 py-3 flex items-center justify-between">
    <div class="font-semibold text-slate-900 flex items-center gap-2">
      <% if (brand.logoUrl) { %><img src="<%= brand.logoUrl %>" alt="<%= brand.name %>" class="h-7 w-auto" /><% } %>
      <span><%= brand.name %></span>
      <% if (business.environment !== 'production') { %><span class="text-xs font-normal text-slate-400">(<%= business.environment %>)</span><% } %>
    </div>
    <div class="flex gap-4 text-sm">
      <a href="/dashboard" class="hover:text-brand">Resumen</a>
      <a href="/dashboard/customers" class="hover:text-brand">Clientes</a>
      <a href="/dashboard/payments" class="hover:text-brand">Pagos</a>
      <a href="/dashboard/notifications" class="hover:text-brand">Notificaciones</a>
      <a href="/dashboard/import" class="hover:text-brand">Importar</a>
      <form action="/logout" method="post" class="inline">
        <button class="text-slate-400 hover:text-red-600">Salir</button>
      </form>
    </div>
  </div>
</nav>
